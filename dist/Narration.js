export class Narration {
    constructor(id, text, duration, audio) {
        this.id = id;
        this.text = text;
        this.duration = duration; // the original duration of the given audio file
        this.audio = audio;
    }
}
//# sourceMappingURL=Narration.js.map